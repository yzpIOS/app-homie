项目初始化流程：

0、在共享机器上：192.168.1.111, 在目录workspace/flutter/android/中, 把unityLibrary工程copy下来，如果文件太大，就叫unity同学把文件打包成压缩包
1、首先把unity项目发布出来（项目名：unityLibrary）, 然后把项目放到android/unityLibrary下面
2、新建一个空的unity项目，然后发布到各个平台，把里面的deploy文件放到android/unityLibrary/src/main/Il2CppOutputProject/IL2CPP/build/deploy
3、在main.dart的Edit Configuration中配置--dart-define-from-file=./env/dev.json


项目注意事项：
Flutter sksl 着色器预热
https://blog.csdn.net/sxt_zls/article/details/119929628


生成protoBuff对应的对像
protoc --dart_out=. Common.proto

开发过程中：
main.dart中的Edit Confitions中配置run args
--dart-define-from-file=./env/debug.json
发布过程中：
--dart-define-from-file=./env/release.json


*****************iOS打包提审流程 start*****************
1.配置项目unityLibrary
1-1.Unity hub — Homie — 打包 — Flutter — 更新（）
1-2.Unity hub — Homie — 打包 — Flutter — 一键导出iOS（）
1-3.将刚刚打包好的UnityLibrary里所有文件复制到flutter项目ios目录下unityLibrary
2.打开Xcode修改MetalHelper.mm文件、PosixWaitObject文件、ThreadImpl、Thread
3.修改pubspec.yaml里的version，修改flutter项目Edit Configuration的环境为release，并build成功
4.打开Xcode — Runner — Flutter — Generated — 查看api_url是否正常
5.修改版本号(两个地方：1.General   2.Build Setting里的FLUTTER_BUILD_NUMBER)
6.正常流程Archive — Distribute App
*****************iOS打包提审流程 end*****************


// 每次发布版本时，把MetalHelper.mm文件内容修改成这个，否则会报错
// MetalHelper.mm

dispatch_semaphore_t inflightSemaphore = dispatch_semaphore_create(3);

extern "C" void PresentMTL(UnityDisplaySurfaceMTL* surface)
{
    if (surface->drawable)
    {
        // for some reason presentDrawable: afterMinimumDuration: is missing from simulator headers completely in xcode 12
    #if (PLATFORM_IOS || PLATFORM_TVOS) && !(TARGET_IPHONE_SIMULATOR || TARGET_TVOS_SIMULATOR)
        const int targetFPS = UnityGetTargetFPS(); assert(targetFPS > 0);
        dispatch_semaphore_wait(inflightSemaphore, DISPATCH_TIME_FOREVER);
        [UnityCurrentMTLCommandBuffer() addCompletedHandler:^(id<MTLCommandBuffer> buffer) {
            if(inflightSemaphore == nil) {
                return;
            }
            dispatch_semaphore_signal(inflightSemaphore);
            return;
        }];
    #endif
        // note that we end up here if presentDrawable: afterMinimumDuration: is not supported
        [UnityCurrentMTLCommandBuffer() presentDrawable: surface->drawable];
    }
}




/// 修改PosixWaitObject文件，ThreadImpl::GetCurrentThread();会返回空
    WaitStatus PosixWaitObject::Wait(uint32_t timeoutMS, bool interruptible)
    {
        // IMPORTANT: This function must be exception-safe! APCs may throw.

        ThreadImpl* currentThread = ThreadImpl::GetCurrentThread();
        if(currentThread == nullptr) {
            return kWaitStatusFailure;
        }
// 修改ThreadImpl
    ThreadImpl* ThreadImpl::GetCurrentThread()
    {
        Thread* currentThread = Thread::GetCurrentThread();
        if(currentThread == nullptr) {
            return nullptr;
        }
        return currentThread->m_Thread;
    }
// 修改Thread
    Thread* Thread::GetCurrentThread()
    {
        void* value;
        s_CurrentThread.GetValue(&value);
        if(value == nullptr) {
            return nullptr;
        }
        return reinterpret_cast<Thread*>(value);
    }